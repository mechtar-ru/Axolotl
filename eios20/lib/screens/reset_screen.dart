import 'package:flutter/material.dart';

class ResetScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Emotional Reset')),
      body: Center(child: Text('6 guided protocols for emotional reset')));
  }
}