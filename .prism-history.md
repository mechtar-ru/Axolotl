# Prism Constraint History
### 2026-05-11 12:40:26 — ContextCurationService.java Analysis
- **Maximized:** Theoretical correctness and identification of structural trade-offs in token estimation logic
- **Sacrificed:** Practical adequacy assessment and empirical validation of current heuristic's sufficiency for Axolotl's actual workload
- **Recommendations:** For gap 1: try /prism-scan with focus on empirical measurement of token estimation error in production; For gap 2: try /prism-scan with focus on lightweight adaptive techniques per content type; For gap 3: try /prism-full for multi-angle coverage including performance impact analysis

### 2026-05-11 16:44 — /prism-scan: Gap 2 — Lightweight Adaptive Techniques Per Content Type
- **Constraint:** Uniform scoring (relevance*0.6+centrality*0.4) and format templates for all entities
- **Fix:** Type-aware scoring (interfaces: 0.7/0.3, abstract: 0.65/0.35, concrete: 0.5/0.5), separate format templates per subtype (interface/abstract/concrete class, method), budget-pressure-based condensation (condensed mode at >2x budget pressure), interface boost in relevance, parameters+imports relevance signals
- **Files changed:** `ContextCurationService.java`, `ContextCurationServiceTest.java`
--

### 2026-06-15 17:30 — Full PRISM Analysis

**Context**: Post Phase 3 (tasks 50-51) + multi-session workflow + plugin bridge + Magic Context integration. 387 tests, 0 failures, 1 skipped. Frontend: 8 pre-existing TS errors (QuickStartDialog.vue only).

---

## HIGH Severity

### 1. AgentNodeStrategy.executeToolAgentNode — 498-line god method
**File**: `backend/src/main/java/com/agent/orchestrator/service/AgentNodeStrategy.java:233-731`
**Metric**: 1111L class, 14 dependencies, 37 imports. executeToolAgentNode alone = 498 lines.
**What it does**: Agent type dispatch (code/doc), tool assembly, FLUTTER auto-scaffold (`build_app`, `flutter create`), FLUTTER WORKFLOW prompt injection, MC RAG retrieve → ContextAssembler, predecessor/plan context, fix pass loop (stale file counting, fix model resolution, `dart analyze`), dual-model planner path, final `buildAndReport`.
**Risk**: Single method handles agent type dispatch AND tool execution AND FLUTTER-specific logic AND error recovery AND reporting. Change in one area risks breaking others.
**Suggested fix**: Extract 3 concerns:
- `FlutterScaffoldHelper` — `buildAppForFlutter()`, `resolveFlutterFixModel()`, `runDartAnalyze()`
- `FixPassOrchestrator` — fix loop with stale file counting, retry logic
- Move `runBash()` to `NodeCommandExecutor`
- Extract `buildAndReport()` into a dedicated class (already 78 lines)

### 2. PipelineStageExecutionService (1070L) — new god class from extraction
**File**: `backend/src/main/java/com/agent/orchestrator/service/PipelineStageExecutionService.java`
**Metric**: 1070L, 10 deps. Was extracted from PipelineServiceImpl (1175→205L). It inherited the complexity.
**What it does**: `runPipelineStages()` (133L), `retryPipeline()` 3 overloads (175+172L, largely duplicated), `resumePipeline()` 2 overloads (196L). All in one class.
**Risk**: `retryPipeline` and `resumePipeline` share stages iteration + execution logic but are separate methods with subtle duplication.
**Suggested fix**: Extract `PipelineRetryService` / `PipelineResumeService` or create a shared `PipelineStageRunner` that `retry`, `resume`, and `runPipelineStages` delegate to.

### 3. ReviewNodeStrategy.executeReviewNode (437 lines) + VerifierNodeStrategy.executeVerifierNode (412 lines)
**Files**: `ReviewNodeStrategy.java:75-512`, `VerifierNodeStrategy.java:74-486`
**Metric**: 437L review method, 412L verifier method. Each >400 lines.
**What they do**: Execute the full review/verification pipeline including context building, LLM calls, plan analysis, error checking.
**Risk**: Both have grown beyond reasonable single-method size. The verifier's auto-rewrite loop is particularly fragile.
**Suggested fix**: Extract `ReviewPipelineExecutor` and `VerificationPipelineExecutor` — each with clear phases (context prep → LLM call → result analysis → retry loop).

---

## MEDIUM Severity

### 4. ToolHandlerService (941 lines) — spreading beyond handler boundaries
**File**: `backend/src/main/java/com/agent/orchestrator/service/ToolHandlerService.java`
**Metric**: 941L. Handles 18+ tool implementations.
**What it does**: `handleReadFile`, `handleWriteFile` (sandbox + post-write validation), `handleBash`, `handleWebSearch`, `handleWebFetch`, `handleMemoryRead/Write/Search`, `handleGit`, `handleBuildApp` (145L including FLUTTER-specific logic), `handleGrep`, `handleGraphQuery`, `handleMCP`, `handleRlmPredict`, `sandbox`, `postWriteValidation`.
**Risk**: 18 concerns in one class. `handleBuildApp` contains FLUTTER `flutter create` + `pub get` + `dart analyze` + `flutter test` logic — duplicated from VerifierNodeStrategy and AgentNodeStrategy.
**Suggested fix**: Split into domain-specific handlers:
- `FileToolHandler` — read/write/directory_read/sandbox
- `BuildToolHandler` — build_app with FLUTTER logic
- `NetworkToolHandler` — web_search/fetch/api
- `MemoryToolHandler` — memory_read/write/search
- `CodeToolHandler` — grep/graph_query/git/diff

### 5. AgentController (736 lines) — too many endpoint groups
**File**: `backend/src/main/java/com/agent/orchestrator/controller/AgentController.java`
**Metric**: 736L, ~15 endpoints, 9 injected services. New session/plan chat endpoint (100+ lines) added inline.
**What it does**: Agent CRUD, schema execution, filesystem access, webhook, workspace validation, MCP test, app creation, session planner chat.
**Risk**: Named "AgentController" but handles schema execution, file access, webhooks, and now session chat — too many unrelated concerns.
**Suggested fix**: Move session/plan to `SessionController`, move file access to `FileController`, keep AgentController for Agent CRUD only.

### 6. Plugin config inconsistency — enabled: true (code) vs false (config)
**File**: `PluginLifecycleManager.java:33` (`@Value("${axolotl.plugins.enabled:true}")`) vs `application.yml:76` (`enabled: false`)
**Risk**: Code default says `true`, config says `false`. Silent inconsistency — plugins are disabled despite code default. If someone removes the config line, plugins suddenly enable.
**Suggested fix**: Align: either set `enabled: true` in `application.yml`, or change code default to `false`.

### 7. Dual model hierarchies — 30 POJO vs 18 @Node graph models
**Paths**: `model/` (24 with @Data) vs `graph/model/` (18 @Node entities)
**What it means**: GraphExecutionRun duplicated from ExecutionRun, GraphWorkflowSchema from WorkflowSchema, GraphPlan from Plan, etc. Most fields duplicated with mapping code in ExecutionRepository and other repositories.
**Risk**: Schema changes require changes in 4+ files (POJO + Graph + Repository + Service layer). Adding a field to ExecutionRun means also adding to GraphExecutionRun and the repository mapping methods. 1 skipped test suggests mapping issues.
**Suggested fix (LOW EFFORT)**: Create shared interfaces for common fields (`HasId`, `HasTimestamps`, `HasGeneratedFiles`). Or use a `@MappedField` annotation pattern.

### 8. QuickStartDialog.vue (811 lines, 8+ pre-existing TS errors)
**File**: `frontend/src/components/studio/QuickStartDialog.vue`
**Metric**: 811L, 8 TS type errors (persisted across multiple sessions).
**Risk**: Type errors are a signal of structural issues — the large `options` field type mismatch and `string|null` propagation affects 4 call sites.
**Suggested fix**: Fix the TS errors (tracked for 3+ sessions now). Extract pipeline generation logic into a `usePipelineGenerator` composable.

---

## LOW Severity

### 9. NodeRouter (686 lines, 16 deps) — routing hub
**File**: `backend/src/main/java/com/agent/orchestrator/service/NodeRouter.java`
**Metric**: 686L, 16 dependency fields. `executeNode()` is 235 lines.
**Risk**: Highest dependency count in the codebase. Handles: strategy dispatch, execution tracking, generatedFiles persistence, MagicContext indexing, WebSocket progress, metrics, config sync.
**Suggested fix**: Moderate — delegate generatedFiles persistence and MagicContext indexing to dedicated services (already partially done with Indexer). The 16 deps are mostly strategy instances via Registry pattern (already improved from Phase 2).

### 10. allow-circular-references: true
**File**: `application.yml:6`
**Context**: Set during Phase 2 to allow the old `NodeExecutor ↔ NodeRouter` circular dependency. This was broken in Phase 2 (NodeRouter now injects strategies directly).
**Risk**: Masks future circular deps that should be caught at compile time.
**Suggested fix**: Remove line 6, test full suite. If all 387 tests pass, the circular dep is truly gone.

### 11. base-path hardcoded to developer path
**File**: `application.yml:47` — `base-path: ${AXOLOTL_APPS_PATH:/Users/evgenijtihomirov/git/Axolotl}`
**Risk**: On another machine, if AXOLOTL_APPS_PATH isn't set, it defaults to this developer's home. Projects would silently write to the wrong location.
**Suggested fix**: Change default to `.` (process working directory) or `user.home`.

### 12. Code duplication across strategies' prompt/context assembly
**Files**: AgentNodeStrategy, ReviewNodeStrategy, VerifierNodeStrategy, DraftNodeStrategy, SchemaBuilderNodeStrategy
**Pattern**: Each strategy has ~20-50 lines of very similar code: collect predecessor results → build context block → interpolate variables → assemble with ContextAssembler.
**Suggested fix**: Extract common `ContextAssemblyHelper` with a single `buildFullContext(schema, nodeId, additionalBlocks)` method.

### 13. StudioView.vue (889 lines, 20+ functions) — no test coverage
**File**: `frontend/src/views/StudioView.vue`
**Risk**: Main workspace view with execution orchestration, review handling, dialog management — zero frontend tests.
**Suggested fix**: Extract execution/review state into composables (`useExecution`, `useReview`).

### 14. New code — untested session plan endpoint
**File**: `AgentController.java` — `POST /schemas/{id}/session/plan`
**Risk**: The session planner chat endpoint has no test coverage. Also SessionChatDialog.vue (280L, new component) has no test.
**Suggested fix**: Add `schemas/{id}/session/plan` test to AgentControllerTest, add frontend component test.

---

## Assessment

**Score**: 6.5/10

**What's good**:
- Phase 3 successfully decomposed the 4 original god classes
- Circular dependency between NodeRouter↔NodeExecutor is broken
- Plugin system is well-structured (6 files, clear boundaries)
- Magic Context integration is clean (Indexer + Retriever pattern)
- 387 tests with 0 failures is solid
- SessionChatDialog is well-factored (280L, Teleport, clean interface)

**What needs attention**:
- **executeToolAgentNode (498L)** is the new god method — most urgent
- **PipelineStageExecutionService (1070L)** inherited god class status from decomposition
- **ToolHandlerService (941L)** is 18 concerns in one file
- **Strategies** all duplicate context assembly patterns
- **QuickStartDialog** has persistent TS errors (3+ sessions)
- **Plugin config** is silently inconsistent
- **No tests** for new session plan endpoint or SessionChatDialog

**Phase 4 target**: Decompose the god methods (extract FLUTTER helper, fix pass orchestrator, context assembly helper, retry/resume extractor). Estimated: 5-7 batches, 2-3 sessions.
