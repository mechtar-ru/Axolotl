# Axolotl Project

**Project root:** `/Users/evgenijtihomirov/git/Axolotl/Axolotl`

All file paths must be relative to this directory or absolute paths under it. Do not guess paths — use `ls`, `glob`, or `grep` to find files before reading them.

## Structure

- `backend/` — Spring Boot 3.2, Java 21, Maven
- `frontend/` — Vue 3 + TypeScript + Vite
- `scripts/` — dev utilities

## Rules

- Run backend: `cd backend && mvn spring-boot:run`
- Run frontend: `cd frontend && npm run dev`
- Never use `/testbed/`, `/workspace/`, or any sandbox paths — they don't exist.
