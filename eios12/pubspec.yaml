name: my_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  very_good_analysis: ^4.0.0
  json_serializable: ^6.0.0
  get_it: ^7.0.0
  dio: ^5.0.0
  path_provider: ^2.0.8
  shared_preferences: ^2.0.13
  flutter_secure_storage: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.0
  json_serializable_build_runner: ^6.0.0